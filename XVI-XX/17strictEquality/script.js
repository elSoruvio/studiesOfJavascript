//  = assignment operator
//  == comparison operator (compare if values are equal)
//  === strict equality (compare if values & datatype are equal)
//  != inequality operator (returns true if the values are not equal)
//  !== strict inequality operator (returns true if the values and datatypes are not equal)

/*
const PI = 3.14;

// using the comparison operator
if(PI == "3.14") {
    console.log("That is PI");
} else {
    console.log("That is not PI");
}

// using the strict equality operator
if(PI === "3.14") {
    console.log("That is PI");
} else {
    console.log("That is not PI");
}

// using the inequality operator
if(PI != "3.14") {
    console.log("That is not PI");
} else {
    console.log("That is PI");
}
*/

// using the strict inequality operator
if(PI != "3.14") {
    console.log("That is not PI");
} else {
    console.log("That is PI");
}

// One case where that is important is
// user input
// because user input tends to be a string datatype
// and you might want it to be a number datatype

